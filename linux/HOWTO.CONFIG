This file is outdated, since config is now locked.

Kernel configuration:

Configurable options:
VMS Memory Management (for MM developers only)
VMS Floppy Driver (for driver developers only)
VMS IDE Driver (for driver developers only)

Do not use:
SMP
HIGHMEM
