pal: probably needs reimplementation

ast delivery: needs to be set up in the corresponding priv level

i/o startio etc: need to handle caller's and caller's caller address context &c

change som vmalloc to kmalloc etc?
